package cs355.mcqueen.keith.shapes;

/**
 * The <code>RotateHandle</code> is a specialized ellipse that is used to draw handles for rotating another shape.
 * <p>
 * Created by keith on 5/14/14.
 */
public class RotateHandle extends Ellipse {
	public RotateHandle(Point location, Size size) {
		super(location, size);
	}
}
